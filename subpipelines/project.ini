[project]
import = input.ini
import = geoprocessing.ini
import = sampling.ini
import = reporting.ini
import = probabilistic.ini
import = functions.ini

[subpipeline noop]
source = in -> select({*}) as out
description = A subpipeline that does nothing. \
    Can be useful for if/else decisions where a subpipeline is conditionally \
    swapped in or out.
